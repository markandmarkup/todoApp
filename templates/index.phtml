<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8"/>
        <title>Fondoo || The Cheesy Todo App</title>
        <link rel="icon" href="./images/Cheese-icon.ico" type="image/icon"/>
        <link href="https://fonts.googleapis.com/css?family=Rubik&display=swap" rel="stylesheet">
        <link rel="stylesheet" type="text/css" href="./css/reset.css">
        <link rel="stylesheet" type="text/css" href="./css/style.css">
        <script src="https://kit.fontawesome.com/3d49cc2df6.js" crossorigin="anonymous"></script>
        <script src="./js/index.js" defer></script>
    </head>
    <body>
    <header>
        <div class="center-container">
            <img class="logo" src="./images/cheese-icon.png" />
            <div class="titles">
                <h1>Fondoo</h1>
                <h4>The cheesy to-do app</h4>
            </div>
        </div>
    </header>
    <div class="center-container">
        <section id="incomplete">
            <h2>My to-dos:</h2>
            <div class="toDoContainer">
                <form action="/" method="post">
                    <input type="text" id="toDoTitle" name="toDoTitle" placeholder="New to-do" />
                    <input type="submit" value="Add" />
                </form>
                <?php foreach($ToDos as $ToDo) :
                    if ($ToDo['completed'] === '0') : ?>
                    <div class="toDo">
                        <button type="button" class="deleteToDo" data-id="<?= $ToDo['id'] ?>">&#10008;</button>
                        <div class="toDoContent">
                            <div>
                                <span><?= $ToDo['title'] ?></span>
                                <button type="button" class="toggleEdit"><i class="fas fa-pencil-alt"></i></button>
                            </div>
                            <form class="editForm">
                                <input type="text" name="editTitleInput" class="editTitleInput" value="<?= $ToDo['title'] ?>" data-id="<?= $ToDo['id'] ?>" />
                                <input type="submit" value="Submit" />
                            </form>
                        </div>
                        <button type="button" class="completeToDo" data-id="<?= $ToDo['id'] ?>">Done!</button>
                    </div>
                    <? endif; ?>
                <? endforeach; ?>
            </div>
        </section>
        <section id="complete">
            <h2>Completed:</h2>
            <div class="toDoContainer">
                <?php foreach($ToDos as $ToDo) :
                    if ($ToDo['completed'] === '1') : ?>
                        <div class="toDo">
                            <button type="button" class="deleteToDo" data-id="<?= $ToDo['id'] ?>">&#10008;</button>
                            <?= $ToDo['title'] ?>
                            <span class="comp-date"><?= $ToDo['comp_date'] ?></span>
                        </div>
                    <? endif; ?>
                <? endforeach; ?>
            </div>
        </section>
    </div>
    <footer>
        <a href="https://www.vecteezy.com/free-vector/cheese-texture">Cheese Texture Vectors by Vecteezy</a>||
        <a href="http://www.kameleon.pics">Cheese Icon by Kameleon</a>
    </footer>
    </body>
</html>